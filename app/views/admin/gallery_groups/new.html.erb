<%= render "partials/header" %>

<section class="py-5">
    <div class="container px-5" style="max-width: 720px;">
        <h1 class="fw-bolder mb-4">New Gallery Group</h1>
        <%= render "form", gallery_group: @gallery_group %>
        <%= link_to "Back", admin_gallery_groups_path, class: "btn btn-link px-0 mt-3" %>
    </div>
</section>

<%= render "partials/footer" %>