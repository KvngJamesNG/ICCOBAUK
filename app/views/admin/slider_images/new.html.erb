<%= render "partials/header" %>

<section class="py-5">
    <div class="container px-5" style="max-width: 900px;">
        <h1 class="fw-bolder mb-4">New Slider Image</h1>
        <%= render "form", slider_image: @slider_image %>
        <%= link_to "Back", admin_slider_images_path, class: "btn btn-link px-0 mt-3" %>
    </div>
</section>

<%= render "partials/footer" %>